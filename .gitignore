# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
.swiftpm/

# Xcode user and derived data
DerivedData/
xcuserdata/
*.xcuserstate
*.xccheckout
*.xcscmblueprint

# Local build and archive outputs
dist/
*.dSYM
*.xcarchive

# Update signing material must remain outside the repository.
*.key
*.privatekey
.sparkle_private_key

# Local interactive release automation. This script uses the current user's
# Keychain credentials and must not be committed.
/releae.sh

# package
/scripts/package-update.sh